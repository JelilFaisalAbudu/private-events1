<% provide(:title, "#{ current_user.username }") %>

<div class="container">
<h1><%= current_user.username.capitalize %></h1>
<div class="my-3 p-3 bg-white rounded shadow-sm">
	<h4 class="border-bottom border-gray pb-2 mb-0">My Events</h4>
	<% if current_user.created_events.count.zero? %>
		<h2 class=" text-center">You have not organized any event yet </h2>
	<% else %>
		<% current_user.created_events.each do | event | %>
			<div class="media text-muted pt-3">
      <svg class="bd-placeholder-img mr-2 rounded" width="32" height="32" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice" focusable="false" role="img" aria-label="Placeholder: 32x32"><title>Placeholder</title><rect width="100%" height="100%" fill="#007bff"/><text x="50%" y="50%" fill="#007bff" dy=".3em">32x32</text></svg>
      <div class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
        <div class="d-flex justify-content-between align-items-center w-100">
          <strong class="text-gray-dark"><%= event.title %></strong>
          <div>
						<button type="button" class="btn btn-sm btn-outline-secondary">
            	<%= link_to 'View Details', event_path(event) %>
          	</button>
						<button type="button" class="btn btn-sm btn-outline-secondary">
            	<%= link_to 'Edit', edit_event_path(event) %>
          	</button>
					</div>
        </div>
      </div>
    </div>
		<% end %>
	<% end %>
</div>

  <div class="my-3 p-3 bg-white rounded shadow-sm">
    <h4 class="border-bottom border-gray pb-2 mb-0">My Attended Events</h4>
		<%@user.attended_events.each do | event |%>
			<div class="media text-muted pt-3">
      	<svg class="bd-placeholder-img mr-2 rounded" width="32" height="32" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice" focusable="false" role="img" aria-label="Placeholder: 32x32"><title>Placeholder</title><rect width="100%" height="100%" fill="#007bff"/><text x="50%" y="50%" fill="#007bff" dy=".3em">32x32</text></svg>
      	<div class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
        	<div class="d-flex justify-content-between align-items-center w-100">
          	
            	<%= link_to event.title, event_path(event)  %>
          	
        	</div>
      	</div>
    	</div>
		<% end %>
    
    <small class="d-block text-right mt-3">
      <%= link_to 'Back To All Events', events_path %>
    </small>
  </div>
</div>